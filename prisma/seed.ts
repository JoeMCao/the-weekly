// Weekly Compass has no seed data — reviews are created on demand.

async function main() {
  console.log("No seed data required.");
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
